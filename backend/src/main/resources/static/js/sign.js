new Vue({
    el: '#app',
    data: {
        id: ''
    },
    methods: {
        onSignIn() {
            // GET /sign-in/{memberName}
        },
        onSignUp() {
            // POST /members/{memberId}
        },
    }
});